import { OwnershipClaimSubmission } from './ownership-claim-submission';

describe('OwnershipClaim', () => {
  it('should create an instance', () => {
    expect(new OwnershipClaimSubmission()).toBeTruthy();
  });
});
