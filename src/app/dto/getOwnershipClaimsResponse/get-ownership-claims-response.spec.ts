import { GetOwnershipClaimsResponse } from './get-ownership-claims-response';

describe('GetOwnershipClaimsResponse', () => {
  it('should create an instance', () => {
    expect(new GetOwnershipClaimsResponse()).toBeTruthy();
  });
});
